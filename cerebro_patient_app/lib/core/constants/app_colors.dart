import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Teal from the Cerebro brain network
  static const Color primary = Color(0xFF14919B);
  static const Color primaryDark = Color(0xFF0D7480);
  static const Color primaryLight = Color(0xFF5EC6CE);

  // Green from the brain gradient tip
  static const Color secondary = Color(0xFF45D97B);
  static const Color secondaryDark = Color(0xFF2DB85E);

  // Accent — bright green node highlight
  static const Color accent = Color(0xFF4CE68A);

  // Light backgrounds with a subtle teal tint
  static const Color background = Color(0xFFF0F7F8);
  static const Color surface = Colors.white;
  static const Color card = Colors.white;

  static const Color textPrimary = Color(0xFF0D2137);
  static const Color textSecondary = Color(0xFF5A6A7E);
  static const Color textHint = Color(0xFF94A3B8);

  static const Color divider = Color(0xFFE2E8F0);
  static const Color border = Color(0xFFCBD5E1);

  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Dark navy from logo background
  static const Color scaffoldDark = Color(0xFF0D2137);
  static const Color surfaceDark = Color(0xFF162D47);

  // Semantic aliases used by payment screens
  static const Color cardBg = Colors.white;
  static const Color textDark = Color(0xFF0D2137);
  static const Color textGrey = Color(0xFF5A6A7E);
  static const Color successLight = Color(0xFFD1FAE5);
}
